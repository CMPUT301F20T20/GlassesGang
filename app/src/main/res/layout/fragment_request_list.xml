<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:name="com.example.glassesgang.Requests"
    android:id="@+id/request_list_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".OwnerBookProfileActivity">

    <ListView
        android:id="@+id/request_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20dp"
        android:divider="@color/borrowerPrimary"
        android:dividerHeight="20dp"
        tools:listitem="@layout/fragment_request" />
</FrameLayout>
